import { ModalNewPayerController } from './newpayer.controller';
import template from './newpayer.html';

export let newpayerComponent = {
  controller: ModalNewPayerController,
  template: template,
  bindings: {
  }
};
